spring.application.name=SmartTradeBot
spring.profiles.active=dev

spring.config.ignore-unresolvable-placeholders=false

# Configurações do bot
bot.enabled=true
bot.trading.interval=30000
bot.risk.max-position-size=0.1

# Servidor
server.port=8080

